node_modules/
dist/

# Contains a Solana private key in plaintext. Never commit.
.photon-wallet.json
.env

# Local launcher — embeds the operator's Helius API key. Never commit.
.claude/run-server.cmd

# Generated run reports
reports/

# Live-trading mutex (transient)
.real-mode.lock

# Bearer token for the local API. Per-install, regenerated if deleted.
.api-token

# Helius / PumpPortal keys saved from UI Settings. Per-install credentials.
.api-keys.json

# Per-machine feature flag state (toggled via /api/flags)
flags.json

# Copy-trader runtime state (tracked wallets, open copy positions)
.copy-trader.json
meme-coin-finder-app/
.vercel
dist-exe/
